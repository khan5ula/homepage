---
import BaseLayout from './BaseLayout.astro'
const { frontmatter } = Astro.props
---

<BaseLayout pageTitle={frontmatter.title}> <h1>
  <article class="prose mt-4">
    <a href="/">← all posts</a>
    <p>{frontmatter.pubDate.toString()}</p>
    <slot />
  </article>
</BaseLayout>
