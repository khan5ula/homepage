---
interface Props {
  tags: string[]
}

const { tags } = Astro.props
---

<div class="flex flex-wrap justify-center text-xs">
  <span>tags:</span>
  <div>
    {
      tags.map((tag) => (
        <a href={`/tags/${tag}`} class="px-1">
          {tag}
        </a>
      ))
    }
  </div>
</div>
